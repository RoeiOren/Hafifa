import express from "express";
const router = express.Router();

// get all groups
router.get('/', (req, res) => {

})

// get by id
router.get('/:id', (req, res) => {

})

// get by group name
router.get('/:name', (req, res) => {

})

export = router;