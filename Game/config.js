var config;

if (gameStarted) { 
    config = {
        GRAVITY: 0.55,
        FAIL_GRAVITY: 0.8,
        IMG_URL: "../images/",   
        SPIKE_WIDTH: canvas.width / 8,
        SPIKE_HEIGHT: canvas.height / 12,
    }
}